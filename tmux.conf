set -g default-shell /bin/bash
set -g default-command "bash -l"

# ==========================================
# General
# ==========================================

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:Tc"

set -g mouse on
set -s set-clipboard on

set -g history-limit 100000
set -sg escape-time 10

# Shipyard controls intent window names.
set -g automatic-rename off
set -g allow-rename off

set -g focus-events on


# ==========================================
# Window / Pane Behavior
# ==========================================

set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

# Units of work must be created through `forge new` so they receive a
# Treehouse lease, intent metadata, watcher, and cleanup record.
unbind c

# ==========================================
# Shipyard Status Bar
# ==========================================

set -g status on
set -g status-position top
set -g status-interval 2
set -g status-justify left

set -g status-left-length 30
set -g status-right-length 80

# Session = project.
set -g status-left " #[bold]#S "

# Window = unit of work.
# @pipeline_badge may contain tmux formatting.
set -g window-status-format \
    " #I:#{?@pipeline_badge,#{E:@pipeline_badge} ,}#W "

set -g window-status-current-format \
    " #[bold][#I:#{?@pipeline_badge,#{E:@pipeline_badge} ,}#W] "

set -g window-status-separator ""

# Local date and time.
set -g status-right "%Y-%m-%d %H:%M "


# ==========================================
# Pane Appearance
# ==========================================

set -g display-panes-time 1500

# Pane = tool.
# Pane title = repository and branch.
set -g pane-border-status top
set -g pane-border-format \
    " #P │ #{pane_current_command} │ #{@shipyard_context} "

set -g pane-active-border-style fg=colour39
set -g pane-border-style fg=colour238


# ==========================================
# Navigation
# ==========================================

# Move between tools.
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Move between units of work.
bind -n S-Left previous-window
bind -n S-Right next-window

# Move between repository sessions.
bind -n S-Up switch-client -p
bind -n S-Down switch-client -n

# Return Treehouse leases after their last tmux window link disappears.
set-hook -g window-unlinked \
    'run-shell -b "~/.config/shipyard/bin/forge reap #{hook_window}"'
